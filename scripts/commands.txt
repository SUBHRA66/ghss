sudo -u postgres createdb project_deb
sudo -u postgres psql -l

npm install @prisma/adapter-pg pg

when creating a service: nest new service-name --skip-git

/********
-> install postgres
-> sudo apt install postgres
-> sudo -u postgres createdb ghss

* git clone
* cd ghss
* npm install
* create .env file
* copy from sample.env
* apply the migrations: 
* cd packages/database
* npx prisma migrate dev
* npx prisma generate
* npm run build (the db package)


